name: Android Build Oficial (Completo)

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Set up JDK 17
      uses: actions/setup-java@v3
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Setup Gradle
      uses: gradle/gradle-build-action@v2
      with:
        gradle-version: '8.2'
        
    # 1. Gera os arquivos (APK e AAB)
    - name: Build Release App
      run: gradle bundleRelease assembleRelease -x lint --stacktrace

    # 2. Assina o AAB (Para a Play Store)
    - name: Assinar AAB (Bundle)
      uses: r0adkll/sign-android-release@v1
      id: sign_aab
      with:
        releaseDirectory: app/build/outputs/bundle/release
        signingKeyBase64: ${{ secrets.SIGNING_KEY }}
        alias: ${{ secrets.ALIAS }}
        keyStorePassword: ${{ secrets.KEY_STORE_PASSWORD }}
        keyPassword: ${{ secrets.KEY_PASSWORD }}

    # 3. Assina o APK (Para instalar no Celular/TV)
    - name: Assinar APK (Instalador)
      uses: r0adkll/sign-android-release@v1
      id: sign_apk
      with:
        releaseDirectory: app/build/outputs/apk/release
        signingKeyBase64: ${{ secrets.SIGNING_KEY }}
        alias: ${{ secrets.ALIAS }}
        keyStorePassword: ${{ secrets.KEY_STORE_PASSWORD }}
        keyPassword: ${{ secrets.KEY_PASSWORD }}

    # 4. Upload do AAB Assinado
    - name: Download AAB (Play Store)
      uses: actions/upload-artifact@v4
      with:
        name: VLTV-Play-PlayStore-AAB
        path: ${{ steps.sign_aab.outputs.signedReleaseFile }}

    # 5. Upload do APK Assinado
    - name: Download APK (Instalador TV e Celular)
      uses: actions/upload-artifact@v4
      with:
        name: VLTV-Play-Instalador-APK
        path: ${{ steps.sign_apk.outputs.signedReleaseFile }}
